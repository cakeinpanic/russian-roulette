name: 'Russian roulette'
description: 'Russian roulette for tests'
inputs:
  slack-token:
    description: Slack token
    required: true
  slack-channel:
    description: Slack token
    required: true
  fixers:
    description: Slack token
    required: true
runs:
  using: 'node12'
  main: 'dist/index.js'
